GoogleEarth Tools
-----------------
View cursor position, selected features or whole layer in GoogleEarth
application.

Plugin allows to create KML file, containing cursor position, selected
feature(s) or whole layer and open it in GoogleEarth desktop application.

NOTE: GoogleEarth application should be installed first.
