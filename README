GoogleEarth Tools
-----------------
View cursor position, selected features or whole layer in Google Earth
application.

Plugin allows to create KML file, containing cursor position, selected
feature(s) or whole vector layer and open it in Google Earth desktop
application. Also it supports raster layers, but currently only those that was
loaded via QGIS gdal provider.

It is possible to specify and use custom styling for all types of vector
features.

NOTE: Google Earth application should be installed first.
